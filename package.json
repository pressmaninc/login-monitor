{
  "name": "login-monitor",
  "version": "1.0.0",
  "main": "login-monitor.js",
  "author": "Yosuke Nakatsukasa <yosuke_nakatsukasa@pressman.ne.jp>",
  "license": "GPL-2.0-or-later",
  "scripts": {
    "build": "yarn build:scss && yarn build:js",
    "build:scss": "node-sass --output-style compressed css/login-monitor.scss css/login-monitor.min.css",
    "build:js": "env BABEL_ENV=production babel js/login-monitor.js --out-file js/login-monitor.min.js"
  },
  "devDependencies": {
    "@babel/core": "^7.1.6",
    "@babel/preset-env": "^7.1.6",
    "babel-preset-minify": "^0.5.0"
  }
}
